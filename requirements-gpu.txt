# GPU build: identical to requirements.txt but with onnxruntime-gpu.
# Use on machines with an NVIDIA GPU + CUDA; the service auto-reports gpu:true.
fastapi==0.115.12
uvicorn==0.34.2
rembg==2.0.66
onnxruntime-gpu==1.20.1
opencv-python-headless==4.10.0.84
pillow==11.1.0
